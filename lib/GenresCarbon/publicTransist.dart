import 'package:flutter/material.dart';
class publicTransist extends StatelessWidget {
  const publicTransist({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
