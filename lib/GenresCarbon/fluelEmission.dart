import 'package:flutter/material.dart';
class flueEmission extends StatelessWidget {
  const flueEmission({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
